<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.solvd.argwinterlab.navigator.db.dao.BusMapper">
    <sql id="getBusTemplate">
        <![CDATA[
            SELECT  TS.ID AS BUS_ID,
                    TS.WEIGHT AS BUS_WEIGHT,
                    TS.MODEL AS BUS_MODEL,
                    TS.NAME AS BUS_NAME,
                    B.LINE AS BUS_LINE
            FROM
                nav_db.TRANSPORTS TS
            LEFT JOIN
                nav_db.BUSES B
                    ON TS.ID = B.TRANSPORT_ID
        ]]>
    </sql>

    <select id="findById" resultMap="BusMap">
        <include refid="getBusTemplate"/>
        <![CDATA[
            WHERE TS.ID = #{id}
        ]]>
    </select>

    <select id="findByName" resultMap="BusMap">
        <include refid="getBusTemplate"/>
        <![CDATA[
            WHERE TS.NAME = #{name}
        ]]>
    </select>

    <select id="findAll" resultMap="BusMap">
        <include refid="getBusTemplate"/>
    </select>

    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        <![CDATA[
            BEGIN;
                 INSERT INTO nav_db.TRANSPORTS (WEIGHT, MODEL, NAME)
                 VALUES (
                     #{weight},
                     #{model},
                     #{name}
                 );
                 INSERT INTO nav_db.BUSES (ID, LINE)
                 VALUES (
                    LAST_INSERT_ID(),
                    #{line}
                 );
            COMMIT;
        ]]>
    </insert>

    <update id="updateById">
        <![CDATA[UPDATE
                    nav_db.TRANSPORTS
        ]]>
        <set>
            <if test="weight != null">
                <![CDATA[ WEIGHT = #{weight}, ]]>
            </if>
            <if test="model != null">
                <![CDATA[ MODEL = #{model}, ]]>
            </if>
            <if test="name != null">
                <![CDATA[ NAME = #{name} ]]>
            </if>
        </set>
        <![CDATA[
            WHERE ID = #{id}
        ]]>
    </update>

    <delete id="delete">
        <![CDATA[
            DELETE FROM nav_db.TRANSPORTS
            WHERE ID = #{id}
        ]]>
    </delete>

    <resultMap id="BusMap" autoMapping="false" type="Bus">
        <id property="id" column="BUS_ID"/>
        <result property="model" column="BUS_MODEL"/>
        <result property="weight" column="BUS_WEIGHT"/>
        <result property="wagons" column="BUS_WAGONS"/>
        <result property="name" column="BUS_NAME"/>
    </resultMap>
</mapper>